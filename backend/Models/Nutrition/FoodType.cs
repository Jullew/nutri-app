namespace NutriApp.Models.Nutrition
{
    public enum FoodType
    {
        Raw,        // Surowy produkt (np. jabłko, marchew)
        Processed,  // Przetworzony produkt (np. jogurt, chleb, ser)
        RecipeBase  // Produkt bazowy do przepisów (np. bulion, mieszanka przypraw)
    }
}
